# Flutter-Project
A cross platform mobile application build using flutter. The application is is used for managing subscriptions of a user in a centralized dashboard and send them reminders before subscription expires.
